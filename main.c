#include <stdio.h>
#include <math.h>
#include "func.h"
int main() {

/*
 * 两个整数之和测试用例
 * int nums[] = {2,7,11,15};
 * int *retval=NULL;
 * retval=twoSum(nums, 4, 26);
 * printf("[%d,%d]\n", *(retval+0), *(retval+1));
 *
 */
    int result = reverse(123);
    printf("%d\n", result);
    return 0;
}